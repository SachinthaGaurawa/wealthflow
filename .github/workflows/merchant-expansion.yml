name: Merchant Expansion Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  expand-merchants:
    name: Expand and Verify Merchant List
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository codebase
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Verify Workspace Structure
        run: |
          ls -la
          echo "Verifying Test directory existence..."
          ls -la Test/ || echo "WARNING: Directory Test/ does not exist. Check case sensitivity."

      - name: Initialize Node.js Environment
        uses: actions/setup-node@v4
        with:
          # Updated to 24 to fix the deprecation warning shown in your logs
          node-version: '24'

      - name: Install Application Dependencies
        # This safely installs your required packages without looking for a lockfile
        run: npm install --no-package-lock || echo "No package.json found, skipping install."

      - name: Expand & verify merchant list
        env:
          APP_URL: ${{ vars.APP_URL }}
          ANTHROPIC_API_KEY: ${{ vars.ANTHROPIC_API_KEY || secrets.ANTHROPIC_API_KEY }}
          ANTHROPIC_MODEL: ${{ vars.ANTHROPIC_MODEL }}
          MERCHANT_BATCH: ${{ vars.MERCHANT_BATCH }}
        run: node Test/merchant-expand.cjs
